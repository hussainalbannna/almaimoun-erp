// ════════════════════════════════════════════════════════════════════
//  التحكّم بالوصول — تعريف "المالك" في مكان واحد (مصدر حقيقة موحّد)
//  مؤسسة الميمون للمقاولات
//
//  سجل نشاط الموظفين (audit_log) للمالك وحده. البريد هنا هو نفسه
//  المستخدَم في سياسة RLS على قاعدة البيانات (S03-b): أي تغيير للمالك
//  يجب أن يُطابَق في الموضعين. حماية الواجهة أدناه طبقةٌ تكميلية —
//  الحارس الفعلي للبيانات هو RLS في القاعدة، فلا تُقرأ صفوف السجل
//  حتى لو تُلاعِب بالواجهة.
// ════════════════════════════════════════════════════════════════════

/** بريد مالك المؤسسة — يجب أن يطابق سياسة audit_log_select في قاعدة البيانات */
export const OWNER_EMAIL = 'hussainalbannna@gmail.com'

/**
 * هل هذا المستخدم هو المالك؟
 * تُقارَن البُريد بعد تنظيف المسافات وتوحيد حالة الأحرف (البريد غير حسّاس للحالة).
 */
export function isOwner(email: string | null | undefined): boolean {
  if (!email) return false
  return email.trim().toLowerCase() === OWNER_EMAIL.toLowerCase()
}
